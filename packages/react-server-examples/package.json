{
  "name": "react-server-examples",
  "private": true,
  "scripts": {
    "test": "gulp test",
    "prepublish": "echo 'all good'",
    "clean": "rimraf npm-debug.log*"
  },
  "devDependencies": {
    "rimraf": "^2.2.8"
  }
}
