'use babel';

import ReactServerAtomPluginView from '../lib/react-server-atom-plugin-view';

describe('ReactServerAtomPluginView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
